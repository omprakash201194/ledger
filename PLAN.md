# Ledger — Implementation Plan

Family Financial Digital Legacy Register. A self-hosted, privacy-first webapp that documents the complete financial picture of a household so trusted persons can act immediately in the event of death or incapacity.

---

## Decisions & Rationale

| Decision | Choice | Reason |
|---|---|---|
| Auth | Spring Security OAuth2 (Google) + JWT | Same pattern as SpendStack — no Firebase. Financial data stays on-homelab. |
| Database | Shared homelab Postgres | Relational model fits perfectly; no NoSQL needed. Redis already covers caching. |
| Credential vault | Not built | Out of scope. `DigitalAccount` stores only where credentials live, never the credentials themselves. |
| Terminology | "Trusted person" instead of "nominee" | More human, less legal-form language. |
| Reminders | In-app alerts only (MVP) | No email plumbing needed for MVP. Spring `@Scheduled` scans due dates nightly. |
| Public exposure | Cloudflare Tunnel | Cloudflared already running in homelab. Add routing rule in Cloudflare dashboard only. |
| NoSQL | Not added | Postgres JSONB covers semi-structured needs. Redis already covers caching. Limited RAM on i3-4150. |

---

## Domain Model

All entities have a `user_id` FK (one principal per account, multi-user = household).

### User
The principal who owns the register.
- `id`, `email`, `name`, `provider` (local/google), `password_hash`
- `date_of_birth`, `pan_number` (masked), `aadhaar_ref` (reference/location only)

### TrustedPerson
People the family must contact in an emergency. Replaces "nominee" throughout.
- `id`, `user_id`, `name`, `relationship`, `phone`, `email`, `type` (FAMILY | ADVISOR | EXECUTOR)
- `notes`

### Asset
Every owned asset — financial and physical.
- `id`, `user_id`, `asset_type` (enum), `description`, `institution`, `account_number` (masked)
- `holding_mode` (SINGLE | JOINT | EITHER_OR_SURVIVOR), `joint_holder_name`
- `trusted_person_id` (registered trusted person), `approx_value`, `value_as_of`
- `document_location`, `remarks`
- **Asset type enum:** SAVINGS_ACCOUNT, FIXED_DEPOSIT, PPF, EPF, MUTUAL_FUND, EQUITY, GOLD, REAL_ESTATE, VEHICLE, OTHER

### Liability
Outstanding debts and obligations.
- `id`, `user_id`, `liability_type` (enum), `lender`, `account_number` (masked)
- `original_amount`, `outstanding_balance`, `emi_amount`, `tenure_end_date`
- `linked_asset_id` (FK to Asset, nullable), `remarks`
- **Liability type enum:** HOME_LOAN, CAR_LOAN, PERSONAL_LOAN, EDUCATION_LOAN, CREDIT_CARD, OTHER

### InsurancePolicy
Life, health, and general insurance.
- `id`, `user_id`, `policy_type` (enum), `insurer`, `policy_number` (masked)
- `life_assured`, `sum_assured`, `premium_amount`, `premium_due_date` (day-of-month + month)
- `trusted_person_id` (beneficiary), `maturity_date`, `document_location`, `remarks`
- **Policy type enum:** TERM_LIFE, WHOLE_LIFE, HEALTH, VEHICLE, PROPERTY, OTHER

### DigitalAccount
Online accounts with location of credentials — never the credentials themselves.
- `id`, `user_id`, `category` (enum), `service_name`, `username`
- `credential_location` (e.g. "In Bitwarden vault"), `two_fa_method`, `recovery_contact`
- `action_on_death` (TRANSFER | ARCHIVE | CLOSE | MEMORIALIZE), `remarks`
- **Category enum:** PASSWORD_MANAGER, EMAIL, BANKING, INVESTMENT, SOCIAL_MEDIA, GOVERNMENT, SUBSCRIPTION, OTHER

### RecurringObligation
Auto-debits and standing instructions that keep running until stopped.
- `id`, `user_id`, `obligation_type` (enum), `payee`, `amount`, `frequency` (enum)
- `due_day` (1–31), `payment_source` (account/card description), `action_on_death` (CONTINUE | CANCEL | REVIEW)
- `remarks`
- **Obligation type enum:** LOAN_EMI, SIP, INSURANCE_PREMIUM, SUBSCRIPTION, UTILITY, OTHER
- **Frequency enum:** MONTHLY, QUARTERLY, HALF_YEARLY, YEARLY

### WillRecord
Metadata and location of the Will — the app never stores Will text.
- `id`, `user_id`, `has_will`, `will_type` (SINGLE | JOINT | NONE)
- `location` (physical location of the original), `executor_id` (FK TrustedPerson)
- `registered_with` (court/notary/registrar), `review_reminder_date`, `notes`

### Alert (in-app only)
Generated by the nightly scheduler. Read-only from the frontend.
- `id`, `user_id`, `alert_type` (enum), `title`, `message`, `source_entity` (e.g. "policy_id:42")
- `due_date`, `is_read`, `created_at`
- **Alert type enum:** INSURANCE_PREMIUM_DUE, EMI_DUE, WILL_REVIEW_DUE, OBLIGATION_REVIEW, ASSET_VALUE_STALE

---

## Backend Architecture

**Package:** `com.omprakashgautam.homelab.ledger`

```
backend/src/main/java/.../ledger/
├── config/
│   ├── SecurityConfig.java         # JWT filter + OAuth2 + CORS
│   └── SchedulerConfig.java        # Enable @Scheduled
├── security/
│   ├── JwtAuthenticationFilter.java
│   ├── JwtService.java
│   ├── OAuth2UserService.java
│   └── OAuth2SuccessHandler.java
├── model/                          # JPA entities (one file per entity above)
├── repository/                     # Spring Data JPA repositories
├── service/
│   ├── AssetService.java
│   ├── LiabilityService.java
│   ├── InsurancePolicyService.java
│   ├── DigitalAccountService.java
│   ├── RecurringObligationService.java
│   ├── TrustedPersonService.java
│   ├── WillRecordService.java
│   └── AlertService.java           # also owns the @Scheduled scanner
├── controller/
│   ├── AuthController.java
│   ├── AssetController.java
│   ├── LiabilityController.java
│   ├── InsurancePolicyController.java
│   ├── DigitalAccountController.java
│   ├── RecurringObligationController.java
│   ├── TrustedPersonController.java
│   ├── WillRecordController.java
│   ├── AlertController.java
│   └── DashboardController.java    # net worth summary endpoint
├── dto/                            # Request/Response DTOs per entity
└── exception/
    ├── GlobalExceptionHandler.java
    └── ResourceNotFoundException.java
```

**Profiles:**
- `local` — Postgres on `localhost:5432`, CORS open to `localhost:5173`
- `k8s` — Postgres on `postgres.homelab.svc.cluster.local:5432`

**Alert scheduler:** runs nightly at 02:30 via `@Scheduled(cron = "0 30 2 * * *")`. Scans:
- InsurancePolicy where `premium_due_date` is within 30 days
- RecurringObligation where `due_day` is within 7 days (monthly)
- WillRecord where `review_reminder_date` is within 30 days
- Asset where `value_as_of` is older than 90 days

---

## Database Schema (Liquibase)

Migrations live at `src/main/resources/db/changelog/changes/`.
Master file: `db.changelog-master.xml`.

Planned changesets (one file each):
```
001-create-users.sql
002-create-trusted-persons.sql
003-create-assets.sql
004-create-liabilities.sql
005-create-insurance-policies.sql
006-create-digital-accounts.sql
007-create-recurring-obligations.sql
008-create-will-records.sql
009-create-alerts.sql
```

All tables prefixed `ldg_` to avoid collisions with SpendStack in the shared Postgres instance.

---

## Frontend Architecture

**Stack:** React 18, Vite, Tailwind CSS, React Router v6, Axios, Zustand

```
frontend/src/
├── main.tsx
├── App.tsx                         # Routes
├── api/                            # Axios instance + one file per resource
│   ├── client.ts                   # base URL, JWT interceptor
│   ├── assets.ts
│   ├── liabilities.ts
│   ├── insurance.ts
│   ├── digitalAccounts.ts
│   ├── recurring.ts
│   ├── trustedPersons.ts
│   ├── will.ts
│   ├── alerts.ts
│   └── dashboard.ts
├── store/
│   ├── authStore.ts                # JWT + user profile (Zustand)
│   └── themeStore.ts
├── components/
│   ├── Layout.tsx                  # Shell with sidebar/bottom-nav
│   ├── BottomNav.tsx               # Mobile nav
│   ├── ProtectedRoute.tsx
│   ├── AlertBadge.tsx              # Unread alert count
│   └── EntityCard.tsx              # Reusable summary card
└── pages/
    ├── LoginPage.tsx
    ├── OAuth2CallbackPage.tsx
    ├── DashboardPage.tsx           # Net worth + unread alerts
    ├── AssetsPage.tsx
    ├── LiabilitiesPage.tsx
    ├── InsurancePage.tsx
    ├── DigitalAccountsPage.tsx
    ├── RecurringPage.tsx
    ├── TrustedPersonsPage.tsx
    ├── WillPage.tsx
    └── AlertsPage.tsx
```

**Routing:**
```
/login
/oauth2/callback
/ (protected, Layout wrapper)
  index → DashboardPage
  /assets
  /liabilities
  /insurance
  /digital-accounts
  /recurring
  /trusted-persons
  /will
  /alerts
```

---

## REST API Summary

All routes under `/api/`. All protected except `/api/auth/**` and `/actuator/health`.

| Method | Path | Description |
|---|---|---|
| POST | `/api/auth/login` | Username/password login → JWT |
| POST | `/api/auth/register` | Create account |
| GET | `/api/oauth2/authorization/google` | Initiate Google OAuth2 |
| GET | `/api/dashboard/net-worth` | Total assets − liabilities + unread alert count |
| GET/POST | `/api/assets` | List / create |
| GET/PUT/DELETE | `/api/assets/{id}` | Read / update / delete |
| GET/POST | `/api/liabilities` | |
| GET/PUT/DELETE | `/api/liabilities/{id}` | |
| GET/POST | `/api/insurance` | |
| GET/PUT/DELETE | `/api/insurance/{id}` | |
| GET/POST | `/api/digital-accounts` | |
| GET/PUT/DELETE | `/api/digital-accounts/{id}` | |
| GET/POST | `/api/recurring` | |
| GET/PUT/DELETE | `/api/recurring/{id}` | |
| GET/POST | `/api/trusted-persons` | |
| GET/PUT/DELETE | `/api/trusted-persons/{id}` | |
| GET/PUT | `/api/will` | Single will record per user |
| GET | `/api/alerts` | All alerts for user, unread first |
| PATCH | `/api/alerts/{id}/read` | Mark alert read |

---

## Kubernetes Manifests (`k8s/`)

```
k8s/
├── backend-deployment.yaml         # Spring Boot pod
├── frontend-deployment.yaml        # nginx pod
├── services.yaml                   # ClusterIP for backend + frontend
├── ingress.yaml                    # ledger.homelab.local → frontend; /api/ → backend
├── configmap.yaml                  # SPRING_PROFILES_ACTIVE, JAVA_TOOL_OPTIONS, FRONTEND_URL
├── secret.yaml.template            # Plaintext template — never committed
└── sealed-secret.yaml              # Sealed with kubeseal — safe to commit
```

**Ingress host:** `ledger.homelab.local`
**TLS:** cert-manager self-signed CA (`homelab-ca-issuer`)

**Secrets needed:**
- `APP_JWT_SECRET` — random 256-bit hex
- `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` — from Google Cloud Console
- `DB_PASSWORD` — reuses existing `postgres-secret` in homelab namespace

---

## Cloudflare Tunnel Exposure

No code changes required. Cloudflared already runs in the `homelab` namespace with a token-based config. Routing lives entirely in the Cloudflare dashboard.

**Steps (done once the app is deployed locally):**
1. Log into Cloudflare Zero Trust → Networks → Tunnels → select your tunnel
2. Public Hostnames → Add hostname:
   - **Subdomain / domain:** `ledger.<yourdomain>.com`
   - **Service type:** HTTPS
   - **URL:** `ledger.homelab.local`
   - **TLS → No TLS Verify:** enabled (internal cert is self-signed)
3. DNS record is created automatically by Cloudflare

The nginx ingress handles the internal TLS. Cloudflare terminates the public-facing TLS with their managed cert.

---

## Build Sequence

### Phase 1 — Backend foundation ✅ DONE (commit `615ca17`, issue #1)
1. `backend/pom.xml` — Spring Boot 3.4.5, Web, Security, OAuth2-client, JPA, Validation, Actuator, Liquibase, PostgreSQL, JWT, Lombok, logstash-logback
2. `application.yml` + `application-local.yml` + `application-k8s.yml`
3. `SecurityConfig` + JWT filter + OAuth2 success handler (ported from SpendStack)
4. Liquibase migrations 001–009 (`ldg_` prefixed tables)
5. JPA entities + repositories + service/controller stubs + `GlobalExceptionHandler`
6. `Dockerfile` (multi-stage `eclipse-temurin:21-jre-alpine`) + `.gitignore`

### Phase 2 — Backend features ✅ DONE (commit `01ccade`, issue #2)
7. Implement `mapFromRequest` / `applyUpdate` in all 6 CRUD services (full field mappings)
8. Implement `AlertService` nightly scanner (insurance premiums, EMIs, will review, stale assets)
9. Response DTOs (replace returning raw JPA entities from controllers)

### Phase 3 — Frontend foundation ✅ DONE (commit `c1e83a8`, issue #3)
10. `npm create vite@latest` — React + TypeScript
11. Tailwind CSS, React Router, Axios, Zustand
12. Auth pages (Login, OAuth2 callback), `ProtectedRoute`, `authStore`
13. `Layout` + `BottomNav`

### Phase 4 — Frontend features ✅ DONE (commit `766bbd0`, issue #4)
14. Pages in order: Dashboard → Assets → Liabilities → Insurance → Recurring → TrustedPersons → DigitalAccounts → Will → Alerts

### Phase 5 — Packaging & deploy
15. `frontend/Dockerfile` + `nginx.conf` (proxy `/api/` to `ledger-backend:8080`)
16. k8s manifests (backend-deployment, frontend-deployment, services, ingress, configmap, sealed-secret)
17. Build images, push to `localhost:30500`, apply manifests, verify at `https://ledger.homelab.local`

### Phase 6 — Public exposure
18. Add public hostname rule in Cloudflare dashboard
19. Verify at `https://ledger.<yourdomain>.com`

---

## What is explicitly out of scope

- Credential vault (encrypted password storage)
- PDF / Excel export
- Dead-man's switch / inactivity trigger
- Nominee mismatch alerts (post-MVP)
- Email / push notifications
- Multi-household / shared access for trusted persons (post-MVP — trusted persons are records only, not app users)

package com.omprakashgautam.homelab.ledger;

import org.junit.jupiter.api.Test;

class LedgerApplicationTest {

    @Test
    void contextLoads() {
        // Smoke test — verifies the application context wires without errors when run with mocks.
        // Full context startup is covered in integration tests.
    }
}
